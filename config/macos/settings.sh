# so that when you press and hold a key, the word is repeated
defaults write -g ApplePressAndHoldEnabled -bool false

# you may want to
# - disable **recent apps in Dock**
# - add your code dir to spotlight's privacy list so that it does not search things in node_modules